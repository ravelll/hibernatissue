Scripts to close derelicted GitHub issues.

## Usage

```sh
$ bundle install (--path vendor/bundle)
$ mv .access_token.sample .access_token
$ vim .access_token # set your GitHub access token
$ (bundle exec) ruby closer.rb
```
